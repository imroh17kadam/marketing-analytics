name: Phase 1 – First Action

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Hello Rohit 👋"
          echo "This is my first GitHub Action"